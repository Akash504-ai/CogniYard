# CogniYard Web Client

This folder contains the React/Vite frontend. Install and run the complete application from the project root:

```bash
npm install
npm run dev
```

See the root [README](../README.md) for MongoDB, environment, demo-account, and end-to-end workflow instructions. Do not install or start this folder separately unless you are developing only the frontend.
